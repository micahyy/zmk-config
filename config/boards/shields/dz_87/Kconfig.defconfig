if SHIELD_DZ_87

config ZMK_KEYBOARD_NAME
    default "czm_dz_87"

# Kconfig defaults only for items that must be guaranteed at Kconfig layer.
# Runtime feature flags (monochrome LEDs / HID indicators / sleep) live in
# dz_87.conf, matching the convention used by in-tree ZMK shields.

endif
