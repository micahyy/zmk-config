#include <dt-bindings/led/led.h>

// WS2812: 17 per-key LEDs, data on P0.26 via SPIM3
// Board-specific overlay (pinctrl/SPI must live here per ZMK docs)
//
// IMPORTANT: No "sleep" pinctrl state for SPIM3. On nRF52840, putting MOSI
// into low-power during deep sleep causes WS2812 to freeze after wake — the
// SPI peripheral never resumes driving the data line correctly.

&pinctrl {
    spi3_default: spi3_default {
        group1 {
            psels = <NRF_PSEL(SPIM_MOSI, 0, 26)>;
        };
    };
};

&spi3 {
    compatible = "nordic,nrf-spim";
    status = "okay";
    pinctrl-0 = <&spi3_default>;
    pinctrl-names = "default";

    led_strip: ws2812@0 {
        compatible = "worldsemi,ws2812-spi";
        reg = <0>;
        spi-max-frequency = <4000000>;
        chain-length = <17>;
        spi-one-frame = <0x70>;
        spi-zero-frame = <0x40>;
        color-mapping = <LED_COLOR_ID_GREEN LED_COLOR_ID_RED LED_COLOR_ID_BLUE>;
    };
};

/ {
    chosen {
        zmk,underglow = &led_strip;
    };
};
